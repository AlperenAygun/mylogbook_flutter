# CHANGELOG

## 0.0.2

* Renamed package to `mylogbook_flutter`.
* Renamed `Logbook` class to `MyLogbook`.
* Renamed `logbook.dart` to `mylogbook.dart`.
* Added `openLogViewer(BuildContext context)` helper method to `MyLogbook`.

## 0.0.1

* Initial release.
